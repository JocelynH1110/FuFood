services:
  db:
    image: postgres:18
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: FufoodDev
    restart: unless-stopped
    volumes:
      - "fufood_db:/var/lib/postgresql/18/docker"
    ports:
      - "5434:5432"

volumes:
  fufood_db: { }